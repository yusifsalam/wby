.venv/
__pycache__/
*.pyc
.pytest_cache/
# pygrib writes .idx sidecar index files next to GRIB files
*.idx

# GRIB fixtures are large (100s of MB) — keep the dir, not its data
testdata/*
!testdata/.gitkeep

# generated by explore.ipynb / notebooks
tile.png
.ipynb_checkpoints/
